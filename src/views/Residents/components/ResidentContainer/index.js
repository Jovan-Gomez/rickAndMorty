import React from 'react'
import ResidentInfo from '../ResidentInfo'

const ResidentsContainer = ({infoResident}) => {
    return (
        <ResidentInfo resident={infoResident}/>
    )
}

export default ResidentsContainer
